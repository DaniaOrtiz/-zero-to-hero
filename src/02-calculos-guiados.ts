const width = 8;
const height = 5;
const area = width * height;

const celsius = 20;
const fahrenheit = (celsius * 1.8) + 32; // °F = (°C × 1.8) + 32 (o bien, °C × 9/5 + 32)

console.log(`Área: ${area}`);
console.log(`${celsius} °C equivalen a ${fahrenheit} °F`);
