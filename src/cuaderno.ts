const product = "Cuaderno";
const unitPrice = 25;
const quantity = 10;
const available = true;
const total = unitPrice * quantity;

console.log(`${product}: ${quantity} x ${unitPrice} = ${total}`);
console.log(`Disponible: ${available}`)